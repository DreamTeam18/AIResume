{
  "hooks": {
    "save-session-context": {
      "command": "./.git/hooks/save-session-context",
      "description": "Save session context to memory when /new is issued",
      "trigger": "/new"
    }
  }
}